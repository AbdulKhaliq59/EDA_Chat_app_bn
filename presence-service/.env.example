PORT=3003

# Database Configuration
DB_HOST=localhost
DB_PORT=5434
DB_USERNAME=presence_user
DB_PASSWORD=presence_password
DB_NAME=presence_db

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT Configuration (same as auth service)
JWT_SECRET=your-secret-key-change-in-production

# Application
NODE_ENV=development
